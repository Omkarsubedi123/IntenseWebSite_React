const Button = () => {
  return (
    <div>
      <button
        style={{ backgroundColor: "green", color: "white" }}
        className="button"
      >
        Get Started
      </button>
    </div>
  );
};

export default Button;
